{
  "name": "survey-analytics-platform",
  "private": true,
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install && python -m pip install -r python-service/requirements.txt",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:python": "python -m uvicorn python-service.app:app --reload --port 8000"
  }
}
